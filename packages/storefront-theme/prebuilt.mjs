import prebuilt from './prebuilt.cjs';

export const {
    PREBUILT_THEME_CATALOG_VERSION,
    PREBUILT_THEMES,
    getPrebuiltThemes,
    getPrebuiltTheme,
    resolvePrebuiltTheme,
    validatePrebuiltThemeRegistry
} = prebuilt;

export default prebuilt;
